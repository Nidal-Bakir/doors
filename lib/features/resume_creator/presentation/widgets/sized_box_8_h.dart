import 'package:flutter/material.dart';

class SizedBox8H extends StatelessWidget {
  const SizedBox8H({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 8.0);
  }
}
